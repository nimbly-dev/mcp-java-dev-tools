/**
 * Deterministic Synthesizer registration and compatibility selection.
 *
 * <p>This package owns built-in registration, metadata validation, duplicate
 * handling, external-module blocking, and selection policy. It does not read
 * process environment values.</p>
 */
package com.nimbly.mcpjavadevtools.server.core.synthesis.registry;
