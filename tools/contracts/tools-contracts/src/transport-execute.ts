export const TRANSPORT_EXECUTE_TOOL_CONTRACT = {
  name: "transport_execute",
  description:
    "Execute a transport request through the MCP wrapper with fail-closed policy enforcement.",
} as const;
